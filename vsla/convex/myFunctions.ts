// VSLA Convex project intentionally contains no sample template functions.
// The app’s real data model is defined in convex/schema.ts and the
// production code uses the project’s own app logic instead of the scaffolded
// template API.
